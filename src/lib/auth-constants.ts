/** Cookie name used by the site-password auth layer. */
export const AUTH_COOKIE_NAME = 'site-auth'

/** Value stored in the auth cookie to indicate a valid session. */
export const AUTH_COOKIE_VALUE = 'authenticated'
